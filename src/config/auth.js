const authConfig = {
  meEndpoint: '/auth/me',
  loginEndpoint: '/jwt/login',
  registerEndpoint: '/jwt/register',
  storageAccessToken: 'admin:accessToken',
  storageUserData: 'admin:userData'
};

export default authConfig;
